[project]
name = "ai-live-demo-backend"
version = "0.1.0"
description = "AI Live Demo backend (Stage 1 skeleton)"
requires-python = ">=3.12,<3.13"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "anthropic>=0.40",
    "python-dotenv>=1.0",
]
